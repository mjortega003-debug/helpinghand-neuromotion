input_source: "ultracortex"    # mock OR ultracortex

mock:
  channels: 8
  duration: 5
  sample_rate: 250

ultracortex:
  serial_port: "com6"      # Windows example; use "/dev/ttyUSB0" on Linux
  pull_samples: 250        # How many samples to pull per read